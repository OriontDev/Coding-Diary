public class Main{
    public static void main(String[] args) {
        String name = "Oriont Fedora";

        int length = name.length();
        char letter = name.charAt(1); //whats the char on index 1?
        int index = name.indexOf("O"); //in what index is O?
        int lastIndex = name.lastIndexOf("O"); //where is the last O?

        //name = name.toUpperCase();
        //name = name.toLowerCase();
        //name = name.trim(); //remove all unecessary whiteblank
        //name = name.replace("a", "o"); //replace a to O

        //here what you can use in if statement

        //name.isEmpty()
        //name.contains("f")
        //name.equals("Fedora Oriont")
        //name.equalsIgnoreCase("Fedora Oriont")

    }
}